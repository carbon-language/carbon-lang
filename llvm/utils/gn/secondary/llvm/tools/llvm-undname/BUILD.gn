executable("llvm-undname") {
  deps = [
    "//llvm/lib/Demangle",
    "//llvm/lib/Support",
  ]
  sources = [
    # Make `gn format` not collapse this, for sync_source_lists_from_cmake.py.
    "llvm-undname.cpp",
  ]
}
