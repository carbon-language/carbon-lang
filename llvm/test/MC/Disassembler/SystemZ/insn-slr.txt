# RUN: llvm-mc --disassemble %s -triple=s390x-linux-gnu | FileCheck %s
# CHECK: slr %r0, %r0
0x1f 0x00

# CHECK: slr %r0, %r15
0x1f 0x0f

# CHECK: slr %r15, %r0
0x1f 0xf0

# CHECK: slr %r7, %r8
0x1f 0x78
