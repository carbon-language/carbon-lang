# Part of the Carbon Language project, under the Apache License v2.0 with LLVM
# Exceptions. See /LICENSE for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

import lit.formats
import os

config.name = 'lit'
config.suffixes = ['.carbon']
config.test_format = lit.formats.ShTest()
config.substitutions.append(('%{prelude}', os.path.join(os.environ["TEST_SRCDIR"], "carbon/executable_semantics/data/prelude.carbon")))
config.substitutions.append(('%{executable_semantics}', os.path.join(os.environ["TEST_SRCDIR"], "carbon/executable_semantics/executable_semantics")))
config.substitutions.append(('%{not}', os.path.join(os.environ["TEST_SRCDIR"], "llvm-project/llvm/not")))
config.substitutions.append(('%{FileCheck}', os.path.join(os.environ["TEST_SRCDIR"], "llvm-project/llvm/FileCheck")))
