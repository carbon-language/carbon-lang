import XCTest
@testable import Syntax

final class TokenizerTests: XCTestCase {
    func testExample() {
      XCTAssertEqual(Greet().text, "Hello, World!")
      // try! parse("var Int: x;")
    }
}
