fn main() -> Int {
  var auto: t = fnty (Int,Int);
  var Int: x = 0;
//  match (t) {
//    case fnty(Int: x, Int: y): z =>
//      x = x - 1;
//  }
  match (t) {
    case fnty(Int,Int): z =>
      x = x + 1;
  }
  match (t) {
    case fnty(Type: a,Type: b) =>
      x = x - 1;
  }
  return x;
}