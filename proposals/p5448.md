# Carbon <-> C++ Interop: Primitive Types

<!--
Part of the Carbon Language project, under the Apache License v2.0 with LLVM
Exceptions. See /LICENSE for license information.
SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
-->

[Pull request](https://github.com/carbon-language/carbon-lang/pull/5448)

<!-- toc -->

## Table of contents

-   [Abstract](#abstract)
-   [Problem](#problem)
-   [Background](#background)
    -   [Carbon Primitive Types](#carbon-primitive-types)
    -   [C++ Fundamental Types](#c-fundamental-types)
        -   [Character types](#character-types)
        -   [Byte](#byte)
        -   [Standard signed integer types](#standard-signed-integer-types)
        -   [Fixed-width signed integer types](#fixed-width-signed-integer-types)
        -   [Other signed integer types](#other-signed-integer-types)
        -   [Unsigned integer types](#unsigned-integer-types)
        -   [Standard floating-point types](#standard-floating-point-types)
        -   [Fixed-width floating-point types (C++23)](#fixed-width-floating-point-types-c23)
    -   [Data models](#data-models)
-   [Proposal](#proposal)
-   [Details](#details)
    -   [Unsupported types](#unsupported-types)
-   [Rationale](#rationale)
-   [Alternatives considered](#alternatives-considered)
-   [Open questions](#open-questions)

<!-- tocstop -->

## Abstract

Define the type mapping of the primitive types between Carbon and C++.

## Problem

This proposal aims to specify the type mapping of primitive types between Carbon
and C++. That supports the interoperability between the two languages, which is
one of the Carbon's goals, as stated in
[Interoperability with and migration from existing C++ code](/docs/project/goals.md#interoperability-with-and-migration-from-existing-c-code)).

## Background

### Carbon Primitive Types

Carbon has the following
[primitive types](https://github.com/carbon-language/carbon-lang/blob/trunk/docs/design/README.md#primitive-types):

-   `bool`: boolean type taking `true` or `false`
-   integer types:
    -   signed integer types: `iN` (`N` - bit width, a positive multiple of 8)
        -   `i8`, `i16`, `i32`, `i64`, `i128`
    -   unsigned integer types: `uN` (`N` - bit width, a positive multiple of 8)
        -   `u8`, `u16`, `u32`, `u64`, `u128`
-   floating-point types: `fN` (`N` - bit width, a positive multiple of 8),
    IEEE-754 format
    -   `f16`, `f32`, and `f64` - always available
    -   `f80`, `f128`, or `f256` may be available, depending on the platform

### C++ Fundamental Types

C++ calls the primitive types
[fundamental types](https://en.cppreference.com/w/cpp/language/types). The
following fundamental types exist in C++:

-   `void`
-   `std::nullptr_t`
-   integral types (also integer types):
    -   `bool`
    -   character types:
        -   narrow character types: `signed char`, `unsigned char`, `char`,
            `char8_t` (c++20)
        -   wide character types: `char16_t`, `char32_t`, `wchar_t`
    -   signed integer types:
        -   standard signed integer types: `signed char`, `short`, `int`,
            `long`, `long long`
        -   extended signed integer types (implementation-defined)
    -   unsigned integer types:
        -   standard unsigned integer types: `unsigned char`, `unsigned short`,
            `unsigned int`, `unsigned long`, `unsigned long long`
        -   extended unsigned integer types
-   floating-point types:
    -   standard floating-point types: `float`, `double`, `long double`
    -   extended floating-point types:
        -   fixed width floating-point types (since C++23): `float16_t`,
            `float32_t`, `float64_t`, `float128_t`, `bfloat16_t`
        -   other implementation-defined extended floating-point types

#### Character types

| Type            | Width in bits                      | Notes                                                                                                                                                                |
| --------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `char`          | 8-bit                              | same representation, alignment and signedness as either `signed char` (for example x86, x64) or `unsigned char` (for example ARM, PowerPC), but it's a distinct type |
| `signed char`   | 8-bit                              | signed character representation                                                                                                                                      |
| `unsigned char` | 8-bit                              | unsigned character representation                                                                                                                                    |
| `char8_t`       | 8-bit                              | UTF-8 character representation; same size, alignment and signedness as `unsigned char`, but a distinct type                                                          |
| `char16_t`      | 16-bit                             | UTF-16 character representation; same size, alignment and signedness as `std::uint_least16_t`, but a distinct type                                                   |
| `char32_t`      | 32-bit                             | UTF-32 character representation; same size, alignment and signedness as `std::uint_least32_t`, but a distinct type                                                   |
| `wchar_t`       | 32-bit on Linux, 16-bit on Windows | wide character representation, holds UTF-32 on Linux and other non-Windows platforms, UTF-16 on Windows.                                                             |

#### Byte

| Type   | Width in bits | Notes                                                                                   |
| ------ | ------------- | --------------------------------------------------------------------------------------- |
| `byte` | 8-bit         | same as `unsigned char` can be used to access raw memory, but it's not a character type |

#### Standard signed integer types

| Type          | Width in bits                                                                              |
| ------------- | ------------------------------------------------------------------------------------------ |
| `signed char` | 8-bit                                                                                      |
| `short`       | 16-bit                                                                                     |
| `int`         | 32-bit (except for [LP32](/proposals/p5448.md#data-models) data model, where it is 16-bit) |
| `long`        | 32-bit (except for [LP64](/proposals/p5448.md#data-models) data model, where it is 64-bit) |
| `long long`   | 64-bit                                                                                     |

#### Fixed-width signed integer types

Typically aliases of the standard integer types, defined as:

| Fixed-width type              | Width in bits                                       | Defined as                                                                                                                                                                                                         |
| ----------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `std::int8_t`                 | 8-bit                                               | `typedef signed char int8_t`                                                                                                                                                                                       |
| `std::int16_t`                | 16-bit                                              | `typedef signed short int16_t`.                                                                                                                                                                                    |
|                               |                                                     | Note: on some platforms (e.g.AVR, MSP430): `typedef signed int int16_t`                                                                                                                                            |
| `std::int32_t`                | 32-bit                                              | `typedef signed int int32_t`                                                                                                                                                                                       |
|                               |                                                     | Note: on some platforms (for example AVR, MSP430): `typedef signed long int32_t`                                                                                                                                   |
| `std::int64_t`                | 64-bit                                              | Depending on the platform, either `typedef signed long int64_t` (for example [LP64](/proposals/p5448.md#data-models)) or `typedef signed long long int64_t` (for example [LLP64](/proposals/p5448.md#data-models)) |
| ----------------------------- |                                                     |                                                                                                                                                                                                                    |
| `std::intptr_t`               | 32-bit on [ILP32](/proposals/p5448.md#data-models); | `typedef int intptr_t` on [ILP32](/proposals/p5448.md#data-models);                                                                                                                                                |
|                               | 64-bit on [LP64](/proposals/p5448.md#data-models)   | `typedef long intptr_t` on [LP64](/proposals/p5448.md#data-models)                                                                                                                                                 |
| ----------------------------- |                                                     |                                                                                                                                                                                                                    |

#### Other signed integer types

| Type        | Width in bits | Notes                                                 |
| ----------- | ------------- | ----------------------------------------------------- |
| `ptrdiff_t` | >=16-bit      | `typedef std::intptr_t ptrdiff_t` (on most platforms) |

#### Unsigned integer types

The unsigned integer types have the same sizes as their
[signed counterparts](/proposals/p5448.md#standard-signed-integer-types).

| Type     | Width in bits | Notes                                          |
| -------- | ------------- | ---------------------------------------------- |
| `size_t` | >=16-bit      | `typedef uintptr_t size_t` (on most platforms) |

#### Standard floating-point types

| Type          | Format                            | Width in bits    | Note                                                                        |
| ------------- | --------------------------------- | ---------------- | --------------------------------------------------------------------------- |
| `float`       | IEEE-754 binary32                 | 32-bits          | The format or the size can vary depending on the compiler and the platform. |
| `double`      | IEEE-754 binary64                 | 64-bits          | The format or the size can vary depending on the compiler and the platform. |
| `long double` | IEEE-754 binary128                | 128-bit          | used by some SPARC, MIPS, ARM64 implementations.                            |
|               | IEEE-754 binary64-extended format | 80-bit or 64-bit | 80-bit (most x86 and x86-64 implementations); 64-bit used by MSVC.          |
|               | `double-double`                   | 128-bit          | used on PowerPC.                                                            |

#### Fixed-width floating-point types (C++23)

They aren’t aliases to the standard floating-point types (`float`, `double`,
`long double`), but to an extended floating-point type.

| Type              | Width in bits | Defined as                     |
| ----------------- | ------------- | ------------------------------ |
| `std::float16_t`  | 16-bit        | `using float16_t = _Float16`   |
| `std::float32_t`  | 32-bit        | `using float32_t = _Float32`   |
| `std::float64_t`  | 64-bit        | `using float64_t = _Float64`   |
| `std::float128_t` | 128-bit       | `using float128_t = _Float128` |
| `std::bfloat16_t` | 16-bit        |                                |

### Data models

The following data models are widely accepted:

-   32-bit systems:
    -   `LP32` (Win16 API): `int` 16-bit; `long` 32-bit; `pointer` 32-bit.
    -   `ILP32` (Win32 API; Unix and Unix-like systems): `int` 32-bit; `long`
        32-bit; `pointer` 32-bit.
-   64-bit systems:
    -   `LLP64` (Win32 API: 64-bit ARM or x86-64, x64): `int` 32-bit; `long`
        32-bit; `pointer` 64-bit.
    -   `LP64` (Unix and Unix-like systems (Linux, macOS)): `int` 32-bit; `long`
        64-bit; `pointer` 64-bit.

As discussed in
[Modern OS platforms, hardware architectures, and environments](/docs/project/principles/success_criteria.md#modern-os-platforms-hardware-architectures-and-environments),
Carbon will prioritize supporting modern OS platforms (e.g modern versions of
Linux (incl. common distributions), Windows, macOS etc.), 64-bit little endian
hardware (for example x86-64, AArch64 (ARM 64-bit), PPC64LE (Power ISA, 64-bit,
Little Endian), RV64I (RISC-V 64-bit)). Historic platforms like the LP32 model
or Non-IEEE 754 binary floating point formats won't be prioritized.

## Proposal

-   C++ `intN_t` types and the standard integer types `signed char`, `short`,
    and `int` will map to the correspondly sized Carbon `iN` type (for example
    `int32_t`, `int` -> `i32`).
-   Carbon `iN` types will map to C++ `intN_t` types (for example `i32` ->
    `int32_t`).
-   `long`, `long long` in C++ will map to new Carbon types (for example
    `Core.Cpp.long/long_long`).
-   Correspondingly, the same will hold for the unsigned types.
-   `float` will map to `f32`; `double` to `f64` and `long double` will map to a
    new Carbon type (for example `Core.Cpp.long_double`).

## Details

This table shows C++ to Carbon type mapping (C++ -> Carbon). Carbon -> C++ will
use the same mapping except that the Carbon `iN` types will map only to the
corresponding C++ `intN_t` types.

| Carbon                        | C++                          |
| ----------------------------- | ---------------------------- |
| none as a return type         | `void`                       |
| `bool`                        | `bool`                       |
| `i8`                          | `int8_t`, `signed char`      |
| `i16`                         | `int16_t`, `short`           |
| `i32`                         | `int32_t`, `int`             |
| `i64`                         | `int64_t`                    |
| `i128`                        | `int128_t`                   |
| `u8`                          | `uint8_t`, `unsigned char`   |
| `u16`                         | `uint16_t`, `unsigned short` |
| `u32`                         | `uint32_t`, `unsigned int`   |
| `u64`                         | `uint64_t`                   |
| `u128`                        | `uint128_t`                  |
| `f32`                         | `float`                      |
| `f64`                         | `double`                     |
| `Core.Cpp.long`               | `long`                       |
| `Core.Cpp.long_long`          | `long long`                  |
| `Core.Cpp.unsigned_long`      | `unsigned long`              |
| `Core.Cpp.unsigned_long_long` | `unsigned long long`         |
| `Core.Cpp.long_double`        | `long double`                |

Note: The names `Core.Cpp.T` represent new Carbon types that don't exist at the
moment and their exact names will still need to be defined.

**Performance**

**Overloading**

**New types**

-   `long`, `long long` -

### Unsupported types

-   C++ `floatN_t` (fixed-width floating-point types) won't be supported at this
    point.

## Rationale

TODO: How does this proposal effectively advance Carbon's goals?

Rather than re-stating the full motivation, this should connect that motivation
back to Carbon's stated goals and principles. This may evolve during review. Use
links to appropriate sections of
[`/docs/project/goals.md`](/docs/project/goals.md), and/or to documents in
[`/docs/project/principles`](/docs/project/principles). For example:

-   [Interoperability with and migration from existing C++ code](/docs/project/goals.md#interoperability-with-and-migration-from-existing-c-code)

## Alternatives considered

TODO: What alternative solutions have you considered? (see alternatives
considered)

-   `long`, `long long`
-   Floating-point types

## Open questions

The mapping of the following types remains open:

-   C++:
    -   `char`, `char8_t`, `char16_t`, `char32_t`, `wchar_t` - Carbon still
        doesn't have character types, so the mapping of these types will be
        discussed once they are available.
    -   the following types will also need a separate discussion for its mapping
        (for example `std::byte`, `intptr_t`, `int_fast8_t`, `int_least8_t`,
        `ptrdiff_t`, `nullptr_t`, `size_t`)
